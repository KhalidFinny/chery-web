      <!-- Image Container - Left side on desktop, Top on mobile -->
      <div class="w-full md:w-1/2 md:pr-8 z-10 pointer-events-auto">
        <div class="md:ml-20 ml-0 relative group">
          <!-- Decorative background elements -->
          <div class="absolute -inset-4 bg-gray-50 rounded-2xl transform rotate-1 group-hover:rotate-0 transition-transform duration-500 ease-out"></div>
          <div class="absolute -inset-2 bg-white rounded-xl transform -rotate-1 group-hover:rotate-0 transition-transform duration-500 ease-out"></div>
          
          <!-- Main image container -->
          <figure class="relative bg-white rounded-xl overflow-hidden transform group-hover:scale-105 transition-transform duration-700 ease-out">
            <img
              :src="item.image"
              :alt="item.model"
              class="w-full h-[300px] md:h-[400px] object-cover"
              loading="lazy"
              width="500"
              height="300"
            />
            
            <!-- Overlay with car info -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/20 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            
            <!-- Floating badge -->
            <div class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm px-3 py-1 rounded-full transform translate-y-2 group-hover:translate-y-0 transition-transform duration-500">
              <span class="text-xs font-semibold text-gray-800">NEW</span>
            </div>
          </figure>
          
          <!-- Decorative dots -->
          <div class="absolute -bottom-2 -right-2 flex space-x-1">
            <div class="w-2 h-2 bg-red-500 rounded-full animate-pulse"></div>
            <div class="w-2 h-2 bg-red-400 rounded-full animate-pulse delay-100"></div>
            <div class="w-2 h-2 bg-red-300 rounded-full animate-pulse delay-200"></div>
          </div>
        </div>
      </div>
      
      <!-- Text container - Right side on desktop, Bottom on mobile -->
      <div class="w-full md:w-1/2 md:pl-8 pt-8 md:pt-0 flex flex-col space-y-6 md:space-y-8 z-10 pointer-events-auto text-center md:text-left">
        
        <!-- Model name with creative typography -->
        <div class="relative">
          <h2 class="font-urbanist text-4xl md:text-6xl font-black tracking-tight text-gray-900 leading-tight" x-text="item.model"></h2>
          <div class="absolute -bottom-2 left-0 md:left-0 w-16 h-1 bg-red-500 transform origin-left scale-x-0 group-hover:scale-x-100 transition-transform duration-700 ease-out"></div>
        </div>
        
        <!-- Price section with modern design -->
        <div class="relative">
          <div class="inline-flex items-center bg-gray-50 px-4 py-2 rounded-full">
            <span class="text-sm font-medium text-gray-600 tracking-wide font-urbanist">Mulai dari</span>
            <span class="ml-2 text-xl md:text-2xl font-bold text-red-600 font-urbanist" x-text="item.detail.Harga.value"></span>
          </div>
        </div>
        
        <!-- Action buttons with new design -->
        <div class="flex flex-col sm:flex-row items-center justify-center md:justify-start space-y-3 sm:space-y-0 sm:space-x-4 mt-4">
          <a href="https://wa.me/6281333292628" target="_blank" rel="noopener noreferrer">
            <button
              @click="console.log('Buy Now clicked')"
              class="group relative font-urbanist text-sm md:text-base px-6 md:px-8 py-3 md:py-4 bg-red-600 text-white rounded-xl tracking-wide transition-all duration-300 ease-out hover:bg-red-700 transform hover:-translate-y-1 z-20 overflow-hidden"
            >
              <span class="relative z-10 flex items-center">
                <i class="fas fa-shopping-cart mr-2"></i>
                <span class="font-semibold">Beli Sekarang</span>
              </span>
              <div class="absolute inset-0 bg-red-700 transform scale-x-0 group-hover:scale-x-100 transition-transform duration-300 ease-out origin-left"></div>
            </button>
          </a>
          
          <button
             @click="window.location.href = item.detail.Button.value.match(/href='([^']+)'/)[1]"      
             class="group relative font-urbanist text-sm md:text-base px-6 md:px-8 py-3 md:py-4 bg-white text-gray-900 border-2 border-gray-200 rounded-xl tracking-wide transition-all duration-300 ease-out hover:border-gray-300 transform hover:-translate-y-1 z-20"
          >
            <span class="relative z-10 flex items-center">
              <i class="fas fa-info-circle mr-2"></i>
              <span class="font-semibold">Pelajari Lebih Lanjut</span>
            </span>
          </button>
        </div>
        
        <!-- Additional info section -->
        <div class="hidden md:block pt-4">
          <div class="flex items-center space-x-6 text-sm text-gray-600">
            <div class="flex items-center space-x-2">
              <i class="fas fa-shield-alt text-red-500"></i>
              <span>5-Star NCAP</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </template>
